  services:
    Tunisel-frontend:
      container_name: Tunisel-frontendcontainer
      build: .
      image: tuniselfrontend:v1
      ports:
        - 80:80
      expose :
          - 80
      networks:
        -internalnet:
  networks:
    internalnet :
      driver: bridge

